---
import SiteLayout from '../layouts/SiteLayout.astro';
import { Code } from 'astro:components';
import heroBackgroundUrl from '../../../assets/isostate-story/hero-tilt-shift-city.png?url';
import editorScreenshotUrl from '../../../assets/isostate-story/editor-overview.png?url';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');

function href(path: string): string {
	return `${base}${path}`;
}

const sceneSnippets = {
	ground: `header:
  layers:
    - {name: ground, order: 0}
    - {name: streets, order: 2}
    - {name: vehicles, order: 5}
floor:
  size: [18, 13]
  visible: true
scenes:
  - id: ground-only`,
	neighborhood: `- id: neighborhood-enters
  add:
    elements:
      - id: start-house
        asset: route-house-start
        layer: buildings
        at: [1, 9]
        size: 2
      - id: end-house
        asset: route-house-end
        at: [15, 6]
        size: 2
    connections:
      - id: main-road
        route: [[3, 8], [14, 8]]
        style: {variant: road}`,
	navigation: `- id: route-appears
  add:
    elements:
      - id: start-label
        asset: text
        at: [3, 7]
        text: {value: Start}
      - id: end-label
        asset: text
        at: [14, 7]
        text: {value: End}
    connections:
      - id: navigation-arrow
        route: [[3, 7], [14, 7]]
        style: {pattern: dotted, stroke: "#2f80ed"}
        start: dot
        end: arrow`,
	carStart: `- id: car-start
  add:
    elements:
      - id: route-car
        asset: route-car
        layer: vehicles
        at: [3, 8]
        enter: slide-in-left
  update:
    connections:
      - id: navigation-arrow
        style:
          pattern: dotted
          strokeWidth: 7
        ambient:
          - name: flow`,
	carMove: `- id: car-center
  update:
    elements:
      - id: route-car
        at: [8, 8]
        ambient:
          - name: pulse
    connections:
      - id: navigation-arrow
        style:
          pattern: dotted
          strokeWidth: 7
        ambient:
          - name: flow`,
	carFinish: `- id: car-finish
  update:
    elements:
      - id: route-car
        at: [14, 8]
        ambient:
          - name: pulse
    connections:
      - id: navigation-arrow
        style: {pattern: dotted}
        ambient:
          - name: flow`,
	arrived: `- id: destination-reached
  add:
    elements:
      - id: arrival-label
        asset: text
        at: [17, 12]
        text:
          value: Arrived
          align: end
          fontSize: 24
  update:
    connections:
      - id: navigation-arrow
        style: {pattern: solid}
        ambient: []`
};
---

<SiteLayout
	title="Isometric 3D scenes from YAML"
	description="isostate turns simple YAML into beautiful scroll-driven isometric visualizations — like Mermaid, but in three dimensions. Compile once, embed anywhere."
>
	<div class="landing">

		<!-- ① HERO ──────────────────────────────────────────────── -->
		<section class="hero" id="overview" aria-label="isostate overview">
			<img class="hero-photo" src={heroBackgroundUrl} alt="" aria-hidden="true" />
			<div class="hero-body">
				<p class="tag">Open source · YAML-first · SVG-native</p>
				<h1>Isometric 3D&nbsp;scenes, written like&nbsp;diagrams.</h1>
				<p class="lead">
					Create beautiful, scroll-driven isometric visuals with simple YAML —
					like writing a Mermaid diagram, but in three dimensions.
					Compile once, embed anywhere. No 3D engine required.
				</p>
				<div class="hero-ctas">
					<a class="cta-primary" href="#story">See it in action</a>
					<a class="cta-ghost" href={href('/docs/README.md')}>Read the docs →</a>
				</div>
				<ul class="hero-pills" aria-label="Key facts">
					<li><strong>YAML</strong> scene authoring</li>
					<li><strong>&lt; 20 KB</strong> runtime</li>
					<li><strong>Zero</strong> browser deps</li>
					<li><strong>SVG</strong> no 3D engine</li>
				</ul>
			</div>
		</section>

		<!-- ② HOW IT WORKS ──────────────────────────────────────── -->
		<section class="how-section" aria-label="How isostate works">
			<div class="how-inner">
				<header class="how-header">
					<p class="eyebrow light">Simple by design</p>
					<h2>Three steps from idea to animation.</h2>
					<p class="how-sub">If you can write a diagram, you can create an isostate scene.</p>
				</header>
				<div class="steps">
					<div class="step">
						<span class="step-n">01</span>
						<h3>Write</h3>
						<p>
							Describe your scene in YAML — elements, layers, connections, transitions.
							Small enough to review in a pull request, expressive enough to tell a full visual story.
						</p>
					</div>
					<div class="step-arrow" aria-hidden="true">
						<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M5 12h14M13 6l6 6-6 6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
					</div>
					<div class="step">
						<span class="step-n">02</span>
						<h3>Compile</h3>
						<p>
							Run isostate at build time. It validates your scene, catches broken links and
							missing assets, then produces a tiny optimised browser bundle — nothing heavy ships.
						</p>
					</div>
					<div class="step-arrow" aria-hidden="true">
						<svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M5 12h14M13 6l6 6-6 6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
					</div>
					<div class="step">
						<span class="step-n">03</span>
						<h3>Publish</h3>
						<p>
							Drop the output on any static host. Choose scroll-driven storytelling,
							step-through presentation mode, or a static snapshot — one YAML source, your format.
						</p>
					</div>
				</div>

				<div class="yaml-preview">
					<p class="yaml-label">A scene description looks like this</p>
					<pre><code>scenes:
  - id: intro
    add:
      elements:
        - id: server
          asset: server-box
          at: [3, 4]
        - id: client
          asset: browser
          at: [7, 4]
      connections:
        - from: &#123;element: client&#125;
          to:   &#123;element: server&#125;
          style: &#123;pattern: dotted, stroke: "#0f5f4f"&#125;
          ambient:
            - name: flow   # animated data flow</code></pre>
				</div>
			</div>
		</section>

		<!-- ③ SCROLL DEMO ───────────────────────────────────────── -->
		<section class="story-grid" id="story" aria-label="Live isostate demo">
			<div class="visual-col">
				<div class="visual-sticky">
					<div class="visual-meta">
						<div class="visual-meta-text">
							<strong id="scene-title">Ground only</strong>
						</div>
						<span class="visual-counter" id="scene-index">01 / 07</span>
					</div>
					<div class="scene-frame">
						<div id="isostate-demo" class="scene-target" aria-label="Animated isostate demo"></div>
					</div>
					<div class="scene-progress-track" aria-hidden="true">
						<span id="scene-progress-bar" class="scene-progress-fill"></span>
					</div>
				</div>
			</div>

			<div class="story-col">
				<div class="story-intro">
					<p class="eyebrow green">See it live</p>
					<h2>Scroll to watch a route come to life.</h2>
					<p class="story-sub">
						The route on the left advances as you read — exactly how it works
						when embedded in your docs or product page.
					</p>
				</div>

				<article class="chapter" data-progress="0" data-title="Ground only" data-index="01 / 07">
					<p class="chapter-n">Scene 01</p>
					<h3>Start with an empty isometric floor.</h3>
					<p>
						The first scene is intentionally quiet: just the SVG floor grid.
						That gives every following object a visible entrance instead of
						starting from a finished diagram.
					</p>
					<ul>
						<li>Floor and layers are defined once in YAML</li>
						<li>No 3D engine or canvas setup required</li>
						<li>The scene can build from nothing as readers scroll</li>
					</ul>
					<div class="chapter-code">
						<div class="chapter-code-head">
							<span>YAML</span>
							<span>scene shell</span>
						</div>
						<Code code={sceneSnippets.ground} lang="yaml" theme="github-dark" wrap />
					</div>
				</article>

				<article class="chapter" data-progress="0.1667" data-title="Neighborhood enters" data-index="02 / 07">
					<p class="chapter-n">Scene 02</p>
					<h3>Bring in the street and houses.</h3>
					<p>
						The route now has context: a straight road, two larger houses, and a few
						trees in the corner. These are ordinary elements and connections added by a scene delta.
					</p>
					<div class="chapter-callout">
						<span class="callout-label">Layering</span>
						<strong>Ground shapes, roads, buildings, labels, and vehicles each sit on explicit layers.</strong>
					</div>
					<div class="chapter-code">
						<div class="chapter-code-head">
							<span>YAML</span>
							<span>add elements</span>
						</div>
						<Code code={sceneSnippets.neighborhood} lang="yaml" theme="github-dark" wrap />
					</div>
				</article>

				<article class="chapter" data-progress="0.3333" data-title="Navigation layer" data-index="03 / 07">
					<p class="chapter-n">Scene 03</p>
					<h3>Add labels and the navigation arrow.</h3>
					<p>
						Start and end labels appear, along with a blue route arrow from origin
						to destination.
					</p>
					<ul>
						<li>Text labels are generated from YAML</li>
						<li>Connections can be arrows, routes, or roads</li>
					</ul>
					<div class="chapter-code">
						<div class="chapter-code-head">
							<span>YAML</span>
							<span>connection</span>
						</div>
						<Code code={sceneSnippets.navigation} lang="yaml" theme="github-dark" wrap />
					</div>
				</article>

				<article class="chapter" data-progress="0.5" data-title="Car starts" data-index="04 / 07">
					<p class="chapter-n">Scene 04</p>
					<h3>Place the car at the start.</h3>
					<p>
						The car enters on the road, not on top of the house. From here the scroll
						position drives a continuous movement along the straight segment.
					</p>
					<div class="chapter-callout">
						<span class="callout-label">Entrance</span>
						<strong>The scene can reveal one object at a time without changing the page layout.</strong>
					</div>
					<div class="chapter-code">
						<div class="chapter-code-head">
							<span>YAML</span>
							<span>element entrance</span>
						</div>
						<Code code={sceneSnippets.carStart} lang="yaml" theme="github-dark" wrap />
					</div>
				</article>

				<article class="chapter" data-progress="0.6667" data-title="Car moves" data-index="05 / 07">
					<p class="chapter-n">Scene 05</p>
					<h3>Move the car and activate the route.</h3>
					<p>
						The car moves from the start of the road to the middle while the
						navigation arrow switches into an animated active state. The route and
						the object tell the same story at the same time.
					</p>
					<ul>
						<li>Element movement is authored as a scene update</li>
						<li>The arrow gives the movement a navigation feel</li>
					</ul>
					<div class="chapter-code">
						<div class="chapter-code-head">
							<span>YAML</span>
							<span>movement</span>
						</div>
						<Code code={sceneSnippets.carMove} lang="yaml" theme="github-dark" wrap />
					</div>
				</article>

				<article class="chapter" data-progress="0.8333" data-title="Car finishes" data-index="06 / 07">
					<p class="chapter-n">Scene 06</p>
					<h3>Drive to the destination.</h3>
					<p>
						The car reaches the end of the road. Keeping the road straight makes the
						interpolation easy to read and avoids awkward corner turns.
					</p>
					<div class="chapter-code">
						<div class="chapter-code-head">
							<span>YAML</span>
							<span>final movement</span>
						</div>
						<Code code={sceneSnippets.carFinish} lang="yaml" theme="github-dark" wrap />
					</div>
				</article>

				<article class="chapter" data-progress="1" data-title="Destination reached" data-index="07 / 07">
					<p class="chapter-n">Scene 07</p>
					<h3>Finish at the destination.</h3>
					<p>
						The destination is highlighted and the final label appears in the bottom
						right of the scene.
					</p>
					<div class="chapter-code">
						<div class="chapter-code-head">
							<span>YAML</span>
							<span>arrival state</span>
						</div>
						<Code code={sceneSnippets.arrived} lang="yaml" theme="github-dark" wrap />
					</div>
				</article>
			</div>
		</section>

		<!-- ④ EDITOR ───────────────────────────────────────────── -->
		<section class="editor-section" aria-label="isostate editor">
			<div class="editor-copy">
				<p class="eyebrow green">Visual authoring</p>
				<h2>Start visually, keep the YAML.</h2>
				<p>
					Use the editor to place assets, tune scenes, and export the same
					`.isostate.yaml` that the CLI validates and compiles. It is the faster
					way to find anchors, object positions, and scene timing without giving
					up source-controlled text.
				</p>
				<div class="editor-actions">
					<a class="cta-primary" href={href('/editor')}>Open the editor</a>
					<a class="cta-ghost" href={href('/mermaid')}>Mermaid workflow →</a>
					<a class="cta-ghost" href={href('/docs/guides/use-editor-in-astro.md')}>Editor docs →</a>
				</div>
			</div>
			<a class="editor-shot" href={href('/editor')} aria-label="Open the isostate editor">
				<img src={editorScreenshotUrl} alt="isostate editor showing the canvas, YAML editor, and asset browser" loading="lazy" decoding="async" />
			</a>
		</section>

		<!-- ⑤ USE CASES ─────────────────────────────────────────── -->
		<section class="use-cases" aria-label="What you can build">
			<div class="section-inner">
				<header class="section-header">
					<p class="eyebrow green">Use cases</p>
					<h2>Anywhere a static diagram feels flat.</h2>
					<p class="section-sub">
						isostate is a generic tool for 3D isometric visualisation. Here are the places
						where visual sequences make the biggest difference.
					</p>
				</header>
				<div class="use-cases-grid">
					<article class="uc-card" style="--accent: var(--green);">
						<span class="uc-num">01</span>
						<h3>Architecture Docs</h3>
						<p>Reveal system complexity one layer at a time. Readers understand each component before the next arrives.</p>
					</article>
					<article class="uc-card" style="--accent: var(--blue);">
						<span class="uc-num">02</span>
						<h3>Product Tours</h3>
						<p>Walk new users through your product visually. Show features in context instead of describing them in text.</p>
					</article>
					<article class="uc-card" style="--accent: var(--yellow);">
						<span class="uc-num">03</span>
						<h3>Presentations</h3>
						<p>Step through scenes like a slide deck. One YAML file, one compelling story for your audience.</p>
					</article>
					<article class="uc-card" style="--accent: var(--mint);">
						<span class="uc-num">04</span>
						<h3>Technical Blogs</h3>
						<p>Give long articles a visual rhythm. Let the diagram build as the explanation unfolds.</p>
					</article>
					<article class="uc-card" style="--accent: var(--red);">
						<span class="uc-num">05</span>
						<h3>Onboarding Flows</h3>
						<p>Show rather than tell. Guide new team members through infrastructure step by step.</p>
					</article>
					<article class="uc-card" style="--accent: var(--green-dark);">
						<span class="uc-num">06</span>
						<h3>Release Notes</h3>
						<p>Make changelogs worth reading. Show what changed, where it fits, and why it matters — visually.</p>
					</article>
				</div>
			</div>
		</section>

		<!-- ⑥ FEATURES ──────────────────────────────────────────── -->
		<section class="features" aria-label="Key features">
			<div class="section-inner">
				<header class="section-header centered">
					<p class="eyebrow green">Why isostate</p>
					<h2>Designed to stay out of your way.</h2>
				</header>
				<div class="features-grid">
					<article class="feat-card">
						<div class="feat-icon">
							<svg width="22" height="22" viewBox="0 0 24 24" fill="none"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8l-6-6z" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/><polyline points="14 2 14 8 20 8" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/><line x1="16" y1="13" x2="8" y2="13" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/><line x1="16" y1="17" x2="8" y2="17" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>
						</div>
						<h3>YAML you can actually read</h3>
						<p>Write scenes the same way you write configuration. No visual editor, no custom syntax — just a format anyone on your team can review and extend.</p>
					</article>
					<article class="feat-card">
						<div class="feat-icon">
							<svg width="22" height="22" viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="1.8"/><line x1="2" y1="12" x2="22" y2="12" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z" stroke="currentColor" stroke-width="1.8"/></svg>
						</div>
						<h3>Scroll or step — you decide</h3>
						<p>Use scroll-driven mode for immersive docs and storytelling, or manual step-through for presentations. Same source file, different controllers.</p>
					</article>
					<article class="feat-card">
						<div class="feat-icon">
							<svg width="22" height="22" viewBox="0 0 24 24" fill="none"><path d="M12 2L2 7l10 5 10-5-10-5z" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/><path d="M2 17l10 5 10-5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/><path d="M2 12l10 5 10-5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></svg>
						</div>
						<h3>Customisable to your brand</h3>
						<p>Theme with CSS variables. Bring your own SVG assets. Choose your colours, stroke widths, and floor styles. isostate doesn't impose a visual style.</p>
					</article>
					<article class="feat-card">
						<div class="feat-icon">
							<svg width="22" height="22" viewBox="0 0 24 24" fill="none"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round"/><polyline points="3.27 6.96 12 12.01 20.73 6.96" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/><line x1="12" y1="22.08" x2="12" y2="12" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>
						</div>
						<h3>Static, portable, fast</h3>
						<p>Under 20 KB gzipped. Deploys to GitHub Pages, Netlify, a CDN, or bundled in an npm package. No server component, no lock-in, no hassle.</p>
					</article>
				</div>
			</div>
		</section>

		<!-- ⑦ GET STARTED ───────────────────────────────────────── -->
		<section class="cta-section" aria-label="Get started with isostate">
			<div class="cta-inner">
				<p class="eyebrow light">Ready to start?</p>
				<h2>Make your docs come&nbsp;alive.</h2>
				<p class="cta-sub">
					You'll have your first scene running in minutes.
					Start with the story-first docs or browse the examples to see what's possible.
				</p>
				<div class="cta-actions">
					<a class="cta-primary" href={href('/docs/README.md')}>Get started</a>
					<a class="cta-ghost-inv" href={href('/docs/examples/runtime-basic.md')}>Browse examples</a>
					<a class="cta-link" href="https://github.com/sebastianwessel/isostate">View on GitHub →</a>
				</div>
			</div>
		</section>

	</div>

	<script>
		import { mountScene } from '../../../packages/core/src/index.ts';
		import scene from '../scenes/product-story.isostate.js';
		import routeCarUrl from '../../../assets/isostate-story/route-car-provided.svg?url';
		import routeBuilding1Url from '../../../assets/isostate-story/route-building-1-provided.svg?url';
		import routeBuilding2Url from '../../../assets/isostate-story/route-building-2-provided.svg?url';
		import routeBuilding3Url from '../../../assets/isostate-story/route-building-3-provided.svg?url';
		import routeBuilding4Url from '../../../assets/isostate-story/route-building-4-provided.svg?url';
		import routeHouseEndUrl from '../../../assets/isostate-story/route-house-end-provided.svg?url';
		import routeHouseStartUrl from '../../../assets/isostate-story/route-house-start-provided.svg?url';
		import routeTreeUrl from '../../../assets/isostate-story/route-tree.svg?url';

		const assetUrls = {
			'route-building-1': routeBuilding1Url,
			'route-building-2': routeBuilding2Url,
			'route-building-3': routeBuilding3Url,
			'route-building-4': routeBuilding4Url,
			'route-car': routeCarUrl,
			'route-house-end': routeHouseEndUrl,
			'route-house-start': routeHouseStartUrl,
			'route-tree': routeTreeUrl
		};

		const target = document.querySelector('#isostate-demo');
		const title = document.querySelector('#scene-title');
		const index = document.querySelector('#scene-index');
		const bar = document.querySelector('#scene-progress-bar');
		const chapters = [...document.querySelectorAll('.chapter')];

		if (target instanceof HTMLElement) {
			initLandingScene(target).catch((error) => {
				target.dataset.error = error instanceof Error ? error.message : String(error);
			});
		}

		async function initLandingScene(target) {
			const runtimeBundle = structuredClone(scene);
			for (const [assetId, url] of Object.entries(assetUrls)) {
				if (runtimeBundle.assets[assetId]) runtimeBundle.assets[assetId].url = url;
			}
			runtimeBundle._digest = await digestBundle(runtimeBundle);

			const mounted = mountScene(target, runtimeBundle, {
				controller: { enabled: true, mode: 'manual' },
				themeVars: {
					'--isostate-floor-stroke': 'rgba(17, 20, 23, 0.16)',
					'--isostate-floor-fill': 'rgba(255, 255, 255, 0.26)'
				}
			});
			mounted.controller?.setProgress(0);

			function setScene(chapter, progress) {
				if (!(chapter instanceof HTMLElement)) return;
				mounted.controller?.setProgress(progress);
				chapters.forEach((item) => item.classList.toggle('is-active', item === chapter));
				if (title) title.textContent = chapter.dataset.title ?? '';
				if (index) index.textContent = chapter.dataset.index ?? '';
				if (bar instanceof HTMLElement) bar.style.inlineSize = `${Math.max(5, progress * 100)}%`;
			}

			let pendingFrame = 0;
			function scheduleScrollUpdate() {
				if (pendingFrame) return;
				pendingFrame = requestAnimationFrame(() => {
					pendingFrame = 0;
					updateSceneFromScroll();
				});
			}

			function updateSceneFromScroll() {
				const chapterStops = chapters
					.filter((chapter) => chapter instanceof HTMLElement)
					.map((chapter) => {
						const rect = chapter.getBoundingClientRect();
						return {
							chapter,
							progress: Number(chapter.dataset.progress ?? '0'),
							anchor: window.scrollY + rect.top + rect.height * 0.42
						};
					});
				if (chapterStops.length === 0) return;

				const marker = window.scrollY + window.innerHeight * 0.52;
				let active = chapterStops[0];
				for (const stop of chapterStops) {
					if (Math.abs(stop.anchor - marker) < Math.abs(active.anchor - marker)) active = stop;
				}

				let progress = chapterStops[0].progress;
				for (let i = 1; i < chapterStops.length; i += 1) {
					const previous = chapterStops[i - 1];
					const next = chapterStops[i];
					if (marker <= next.anchor) {
						const range = Math.max(1, next.anchor - previous.anchor);
						const local = Math.min(1, Math.max(0, (marker - previous.anchor) / range));
						progress = previous.progress + (next.progress - previous.progress) * local;
						break;
					}
					progress = next.progress;
				}

				setScene(active.chapter, progress);
			}

			window.addEventListener('scroll', scheduleScrollUpdate, { passive: true });
			window.addEventListener('resize', scheduleScrollUpdate);
			updateSceneFromScroll();
		}

		async function digestBundle(bundle) {
			const { _digest, ...unsignedBundle } = bundle;
			const encoded = new TextEncoder().encode(canonicalStringify(unsignedBundle));
			const bytes = new Uint8Array(await crypto.subtle.digest('SHA-256', encoded));
			return [...bytes].map((byte) => byte.toString(16).padStart(2, '0')).join('');
		}

		function canonicalStringify(value) {
			return JSON.stringify(normalizeValue(value));
		}

		function normalizeValue(value) {
			if (Array.isArray(value)) return value.map((item) => (item === undefined ? null : normalizeValue(item)));
			if (!value || typeof value !== 'object' || Object.getPrototypeOf(value) !== Object.prototype) return value;
			return Object.fromEntries(
				Object.keys(value)
					.sort()
					.filter((key) => value[key] !== undefined)
					.map((key) => [key, normalizeValue(value[key])])
			);
		}
	</script>

	<style>
		/* ── LANDING WRAPPER ─────────────────────────────────── */
		.landing {
			overflow: clip;
		}

		/* ── SHARED EYEBROW / LABEL ───────────────────────────── */
		.eyebrow {
			display: inline-block;
			margin: 0 0 14px;
			font-size: 0.72rem;
			font-weight: 700;
			letter-spacing: 0.07em;
			text-transform: uppercase;
		}

		.eyebrow.green {
			color: var(--green-dark);
		}

		.eyebrow.light {
			color: var(--mint);
		}

		/* ── HERO ────────────────────────────────────────────── */
		.hero {
			position: relative;
			display: flex;
			align-items: flex-end;
			min-height: calc(100svh - 64px);
			padding: clamp(56px, 8vw, 108px) clamp(24px, 5vw, 80px);
			overflow: hidden;
			background: #071612;
			isolation: isolate;
		}

		.hero::before,
		.hero::after {
			position: absolute;
			inset: 0;
			z-index: -1;
			content: "";
			pointer-events: none;
		}

		.hero::before {
			background:
				linear-gradient(90deg, rgba(5, 18, 15, 0.92) 0%, rgba(5, 18, 15, 0.72) 34%, rgba(5, 18, 15, 0.24) 66%, rgba(5, 18, 15, 0.14) 100%),
				linear-gradient(180deg, rgba(5, 18, 15, 0.3) 0%, rgba(5, 18, 15, 0.08) 48%, rgba(5, 18, 15, 0.82) 100%);
		}

		.hero::after {
			background:
				radial-gradient(ellipse 48% 38% at 76% 46%, rgba(231, 189, 69, 0.18), transparent 65%),
				linear-gradient(90deg, rgba(120, 217, 186, 0.1), transparent 44%);
			mix-blend-mode: screen;
			opacity: 0.82;
		}

		.hero-photo {
			position: absolute;
			inset: 0;
			z-index: -2;
			width: 100%;
			height: 100%;
			object-fit: cover;
			object-position: 61% 48%;
			transform: scale(1.035);
			animation: hero-zoom 18s ease-in-out infinite alternate;
			filter: saturate(1.08) contrast(1.04);
		}

		.hero-body {
			position: relative;
			z-index: 1;
			width: min(760px, 100%);
		}

		.tag {
			display: inline-flex;
			align-items: center;
			gap: 6px;
			padding: 5px 14px;
			margin-bottom: 22px;
			border-radius: 999px;
			border: 1px solid rgba(120, 217, 186, 0.36);
			background: rgba(5, 18, 15, 0.48);
			color: #b7f3dc;
			backdrop-filter: blur(18px);
			-webkit-backdrop-filter: blur(18px);
			font-size: 0.72rem;
			font-weight: 700;
			letter-spacing: 0.06em;
			text-transform: uppercase;
		}

		h1 {
			margin: 0;
			font-size: clamp(3rem, 6.2vw, 7rem);
			font-weight: 900;
			line-height: 0.9;
			letter-spacing: -0.025em;
			font-family: var(--font-display);
			color: #fffdf5;
			text-shadow: 0 5px 28px rgba(0, 0, 0, 0.46);
		}

		.lead {
			margin: 24px 0 0;
			max-width: 560px;
			color: rgba(255, 253, 245, 0.84);
			text-shadow: 0 3px 18px rgba(0, 0, 0, 0.34);
			font-size: clamp(1rem, 1.4vw, 1.2rem);
			line-height: 1.65;
		}

		.hero-ctas {
			display: flex;
			flex-wrap: wrap;
			gap: 12px;
			margin-top: 32px;
		}

		.cta-primary {
			display: inline-flex;
			align-items: center;
			height: 48px;
			padding: 0 26px;
			background: var(--green);
			color: #fffdf5 !important;
			font-weight: 600;
			font-size: 0.95rem;
			text-decoration: none;
			border-radius: 10px;
			box-shadow: 0 4px 16px rgba(15, 95, 79, 0.28);
			transition: background 150ms, transform 150ms, box-shadow 150ms;
		}

		.cta-primary:hover {
			background: var(--green-dark);
			transform: translateY(-2px);
			box-shadow: 0 8px 24px rgba(15, 95, 79, 0.32);
		}

		.cta-ghost {
			display: inline-flex;
			align-items: center;
			height: 48px;
			padding: 0 26px;
			background: rgba(255, 253, 245, 0.14);
			color: #fffdf5 !important;
			font-weight: 600;
			font-size: 0.95rem;
			text-decoration: none;
			border-radius: 10px;
			border: 1.5px solid rgba(255, 253, 245, 0.34);
			backdrop-filter: blur(18px);
			-webkit-backdrop-filter: blur(18px);
			transition: border-color 150ms, background 150ms, transform 150ms;
		}

		.cta-ghost:hover {
			border-color: rgba(255, 253, 245, 0.62);
			background: rgba(255, 253, 245, 0.24);
			transform: translateY(-2px);
		}

		.hero-pills {
			display: flex;
			flex-wrap: wrap;
			gap: 8px;
			margin: 28px 0 0;
			padding: 0;
			list-style: none;
		}

		.hero-pills li {
			display: inline-flex;
			align-items: center;
			gap: 5px;
			padding: 5px 13px;
			border: 1px solid rgba(255, 253, 245, 0.18);
			border-radius: 999px;
			background: rgba(5, 18, 15, 0.4);
			color: rgba(255, 253, 245, 0.72);
			backdrop-filter: blur(14px);
			-webkit-backdrop-filter: blur(14px);
			font-size: 0.73rem;
			font-family: "SFMono-Regular", Consolas, monospace;
		}

		.hero-pills strong {
			color: #a8e6cc;
			font-size: 0.85rem;
		}

		/* ── HOW IT WORKS ────────────────────────────────────── */
		.how-section {
			background: #0a2e24;
			color: #f0ece0;
			padding: clamp(64px, 9vw, 110px) clamp(24px, 5vw, 80px);
		}

		.how-inner {
			max-width: 1360px;
			margin: 0 auto;
		}

		.how-header {
			max-width: 680px;
			margin-bottom: 56px;
		}

		.how-header h2 {
			margin: 0;
			font-size: clamp(2rem, 4vw, 4rem);
			font-weight: 800;
			line-height: 1;
			color: #fffdf5;
			font-family: var(--font-display);
			letter-spacing: -0.02em;
		}

		.how-sub {
			margin: 16px 0 0;
			color: rgba(240, 236, 224, 0.6);
			font-size: 1.05rem;
			line-height: 1.65;
		}

		.steps {
			display: grid;
			grid-template-columns: 1fr auto 1fr auto 1fr;
			gap: 0;
			align-items: start;
		}

		.step {
			padding: 32px 20px;
			border-top: 2px solid rgba(120, 217, 186, 0.2);
		}

		.step-n {
			display: inline-block;
			margin-bottom: 14px;
			color: var(--yellow);
			font-size: 0.72rem;
			font-weight: 800;
			letter-spacing: 0.1em;
			font-family: "SFMono-Regular", Consolas, monospace;
		}

		.step h3 {
			margin: 0 0 12px;
			color: #fffdf5;
			font-size: 1.45rem;
			font-weight: 700;
			font-family: var(--font-display);
			letter-spacing: -0.01em;
		}

		.step p {
			margin: 0;
			color: rgba(240, 236, 224, 0.68);
			line-height: 1.65;
			font-size: 0.94rem;
		}

		.step-arrow {
			display: flex;
			align-items: flex-start;
			padding-top: 42px;
			padding-inline: 12px;
			color: rgba(120, 217, 186, 0.35);
		}

		.yaml-preview {
			margin-top: 52px;
			padding: 28px 32px;
			background: rgba(0, 0, 0, 0.22);
			border: 1px solid rgba(120, 217, 186, 0.14);
			border-radius: 12px;
		}

		.yaml-label {
			margin: 0 0 14px;
			color: rgba(240, 236, 224, 0.42);
			font-size: 0.72rem;
			font-weight: 700;
			text-transform: uppercase;
			letter-spacing: 0.06em;
		}

		.yaml-preview pre {
			margin: 0;
			overflow-x: auto;
			color: #a8e6cc;
			font-family: "SFMono-Regular", Consolas, "Courier New", monospace;
			font-size: 0.84rem;
			line-height: 1.75;
		}

		/* ── SCROLL DEMO ─────────────────────────────────────── */
		.story-grid {
			display: grid;
			grid-template-columns: minmax(0, 1.05fr) minmax(380px, 0.95fr);
			gap: clamp(24px, 4vw, 64px);
			max-width: 1560px;
			margin: 0 auto;
			padding: 80px clamp(24px, 5vw, 80px) 100px;
			border-top: 1px solid rgba(15, 95, 79, 0.1);
		}

		.visual-col {
			min-width: 0;
		}

		.visual-sticky {
			position: sticky;
			top: 88px;
		}

		.visual-meta {
			display: flex;
			align-items: flex-start;
			justify-content: space-between;
			gap: 16px;
			margin-bottom: 20px;
		}

		.visual-label {
			margin: 0 0 4px;
			color: var(--faint);
			font-size: 0.68rem;
			font-weight: 700;
			text-transform: uppercase;
			letter-spacing: 0.06em;
		}

		.visual-meta-text strong {
			display: block;
			font-size: clamp(1.15rem, 1.8vw, 1.6rem);
			font-weight: 700;
			letter-spacing: -0.01em;
			font-family: var(--font-display);
		}

		.visual-counter {
			flex-shrink: 0;
			padding: 5px 11px;
			border: 1px solid rgba(15, 95, 79, 0.18);
			border-radius: 7px;
			background: rgba(255, 253, 245, 0.7);
			color: var(--green-dark);
			font-size: 0.72rem;
			font-weight: 700;
			font-family: "SFMono-Regular", Consolas, monospace;
		}

		.scene-frame {
			position: relative;
			min-height: clamp(480px, 52vw, 740px);
			margin: 0 0 14px;
			overflow: visible;
		}

		.scene-target {
			position: absolute;
			inset: -4% -6% -2%;
		}

		.scene-target :global(svg) {
			width: 100%;
			height: 100%;
			filter: drop-shadow(0 16px 32px rgba(15, 95, 79, 0.1));
		}

		.scene-progress-track {
			height: 5px;
			border-radius: 999px;
			background: rgba(15, 95, 79, 0.1);
			overflow: hidden;
			margin-bottom: 14px;
		}

		.scene-progress-fill {
			display: block;
			height: 100%;
			inline-size: 5%;
			border-radius: 999px;
			background: linear-gradient(90deg, var(--green), var(--mint), var(--yellow));
			transition: inline-size 240ms ease;
		}

		.pipeline-strip {
			display: flex;
			align-items: center;
			gap: 4px;
			flex-wrap: wrap;
			color: var(--faint);
			font-family: "SFMono-Regular", Consolas, monospace;
			font-size: 0.67rem;
			text-transform: uppercase;
			letter-spacing: 0.02em;
		}

		.pipeline-strip span {
			padding: 4px 8px;
			border: 1px solid rgba(15, 95, 79, 0.12);
			border-radius: 5px;
			background: rgba(255, 255, 255, 0.55);
		}

		.pipeline-strip .pipe-sep {
			border: none;
			background: none;
			padding: 0 2px;
			color: rgba(15, 95, 79, 0.25);
		}

		.story-col {
			display: flex;
			flex-direction: column;
		}

		.story-intro {
			padding: clamp(20px, 3vw, 40px) clamp(20px, 3vw, 40px) 48px;
		}

		.story-intro h2 {
			margin: 8px 0 14px;
			font-size: clamp(1.9rem, 3.2vw, 3.2rem);
			font-weight: 800;
			line-height: 1.0;
			font-family: var(--font-display);
			letter-spacing: -0.02em;
		}

		.story-sub {
			margin: 0;
			color: var(--soft-ink);
			font-size: 0.97rem;
			line-height: 1.65;
		}

		.chapter {
			min-height: 70vh;
			padding: clamp(24px, 3.5vw, 44px);
			border-left: 3px solid rgba(15, 95, 79, 0.1);
			border-radius: 0 10px 10px 0;
			transition: border-color 180ms ease, background 180ms ease, transform 180ms ease;
		}

		.chapter.is-active {
			border-color: var(--green);
			background: rgba(255, 255, 255, 0.55);
		}

		.chapter-n {
			margin: 0 0 10px;
			color: var(--green-dark);
			font-size: 0.68rem;
			font-weight: 700;
			letter-spacing: 0.08em;
			text-transform: uppercase;
		}

		.chapter h3 {
			margin: 0 0 14px;
			font-size: clamp(1.9rem, 3.2vw, 3.2rem);
			font-weight: 800;
			line-height: 0.96;
			letter-spacing: -0.02em;
			font-family: var(--font-display);
			color: var(--ink);
		}

		.chapter p {
			margin: 0;
			color: var(--soft-ink);
			font-size: 0.97rem;
			line-height: 1.68;
		}

		.chapter ul {
			margin: 16px 0 0;
			padding-left: 18px;
			color: var(--soft-ink);
		}

		.chapter li {
			line-height: 1.65;
			margin-bottom: 6px;
			font-size: 0.97rem;
		}

		.chapter-callout {
			margin-top: 22px;
			padding: 16px 20px;
			background: rgba(231, 189, 69, 0.12);
			border: 1px solid rgba(231, 189, 69, 0.28);
			border-radius: 9px;
		}

		.callout-label {
			display: block;
			margin-bottom: 5px;
			font-size: 0.68rem;
			font-weight: 700;
			text-transform: uppercase;
			letter-spacing: 0.06em;
			color: #8a6a1a;
		}

		.chapter-callout strong {
			display: block;
			color: var(--ink);
			font-size: 0.93rem;
			line-height: 1.55;
		}

		.chapter-code {
			margin-top: 22px;
			border: 1px solid rgba(15, 95, 79, 0.14);
			border-radius: 10px;
			background: #0f1c18;
			box-shadow: 0 12px 30px rgba(15, 28, 24, 0.1);
			overflow: hidden;
		}

		.chapter-code-head {
			display: flex;
			align-items: center;
			justify-content: space-between;
			gap: 14px;
			padding: 9px 13px;
			border-bottom: 1px solid rgba(120, 217, 186, 0.12);
			background: rgba(255, 255, 255, 0.035);
			color: rgba(200, 240, 224, 0.72);
			font-family: "SFMono-Regular", Consolas, monospace;
			font-size: 0.68rem;
			font-weight: 700;
			letter-spacing: 0.06em;
			text-transform: uppercase;
		}

		.chapter-code-head span:last-child {
			color: rgba(200, 240, 224, 0.38);
			font-weight: 600;
		}

		.chapter-code :global(pre) {
			margin: 0;
			padding: 16px 18px 18px;
			overflow-x: auto;
			background: transparent !important;
			font-size: clamp(0.72rem, 1.2vw, 0.82rem);
			line-height: 1.65;
		}

		.chapter-code :global(code) {
			font-family: "SFMono-Regular", Consolas, "Courier New", monospace;
		}

		.chapter-links {
			display: flex;
			flex-wrap: wrap;
			gap: 10px;
			margin-top: 22px;
		}

		.chapter-links a {
			display: inline-flex;
			align-items: center;
			height: 38px;
			padding: 0 16px;
			background: rgba(255, 253, 245, 0.85);
			border: 1.5px solid rgba(15, 95, 79, 0.18);
			border-radius: 8px;
			color: var(--ink);
			font-size: 0.87rem;
			font-weight: 600;
			text-decoration: none;
			transition: background 140ms, border-color 140ms, transform 140ms;
		}

		.chapter-links a:hover {
			background: white;
			border-color: var(--green);
			transform: translateY(-1px);
		}

		/* ── EDITOR ──────────────────────────────────────────── */
		.editor-section {
			display: grid;
			grid-template-columns: minmax(280px, 0.78fr) minmax(420px, 1.22fr);
			gap: clamp(28px, 5vw, 72px);
			align-items: center;
			padding: clamp(72px, 10vw, 128px) clamp(24px, 5vw, 80px);
			background: #fffdf5;
			border-top: 1px solid rgba(15, 95, 79, 0.08);
		}

		.editor-copy {
			max-width: 520px;
		}

		.editor-copy h2 {
			margin: 0;
			font-family: var(--font-display);
			font-size: clamp(2.3rem, 4vw, 4.4rem);
			line-height: 0.95;
			color: var(--ink);
		}

		.editor-copy p:not(.eyebrow) {
			margin: 20px 0 0;
			color: var(--soft-ink);
			font-size: 1.05rem;
			line-height: 1.7;
		}

		.editor-actions {
			display: flex;
			flex-wrap: wrap;
			gap: 12px;
			margin-top: 30px;
		}

		.editor-shot {
			display: block;
			overflow: hidden;
			border: 1px solid rgba(16, 20, 21, 0.12);
			border-radius: 10px;
			background: #eef1f4;
			box-shadow: 0 24px 70px rgba(16, 20, 21, 0.16);
			text-decoration: none;
			transform: rotate(-1deg);
			transition:
				transform 180ms ease,
				box-shadow 180ms ease;
		}

		.editor-shot:hover {
			transform: rotate(0deg) translateY(-3px);
			box-shadow: 0 30px 90px rgba(16, 20, 21, 0.2);
		}

		.editor-shot img {
			display: block;
			width: 100%;
			height: auto;
			aspect-ratio: 16 / 10;
			object-fit: cover;
			object-position: 50% 50%;
		}

		/* ── USE CASES ───────────────────────────────────────── */
		.use-cases {
			background: #fffdf5;
			padding: clamp(64px, 9vw, 110px) clamp(24px, 5vw, 80px);
		}

		.section-inner {
			max-width: 1360px;
			margin: 0 auto;
		}

		.section-header {
			max-width: 640px;
			margin-bottom: 48px;
		}

		.section-header.centered {
			max-width: 100%;
			text-align: center;
			margin-bottom: 48px;
		}

		.section-header h2 {
			margin: 8px 0 14px;
			font-size: clamp(2rem, 3.5vw, 3.5rem);
			font-weight: 800;
			line-height: 1.0;
			font-family: var(--font-display);
			letter-spacing: -0.02em;
		}

		.section-sub {
			margin: 0;
			color: var(--soft-ink);
			font-size: 0.97rem;
			line-height: 1.65;
		}

		.use-cases-grid {
			display: grid;
			grid-template-columns: repeat(3, 1fr);
			gap: 14px;
		}

		.uc-card {
			padding: 28px;
			border: 1.5px solid rgba(15, 95, 79, 0.12);
			border-radius: 12px;
			background: var(--paper);
			transition: border-color 160ms, transform 160ms, background 160ms, box-shadow 160ms;
			cursor: default;
		}

		.uc-card:hover {
			border-color: var(--accent);
			transform: translateY(-3px);
			background: white;
			box-shadow: 0 10px 28px rgba(15, 95, 79, 0.08);
		}

		.uc-num {
			display: inline-block;
			margin-bottom: 14px;
			color: var(--accent);
			font-size: 0.72rem;
			font-weight: 800;
			letter-spacing: 0.08em;
			font-family: "SFMono-Regular", Consolas, monospace;
		}

		.uc-card h3 {
			margin: 0 0 10px;
			font-size: 1.05rem;
			font-weight: 700;
			color: var(--ink);
		}

		.uc-card p {
			margin: 0;
			color: var(--muted);
			font-size: 0.9rem;
			line-height: 1.62;
		}

		/* ── FEATURES ────────────────────────────────────────── */
		.features {
			padding: clamp(64px, 9vw, 110px) clamp(24px, 5vw, 80px);
			border-top: 1px solid rgba(15, 95, 79, 0.08);
		}

		.features-grid {
			display: grid;
			grid-template-columns: repeat(2, 1fr);
			gap: 14px;
			max-width: 1360px;
			margin: 0 auto;
		}

		.feat-card {
			padding: 32px;
			border: 1.5px solid rgba(15, 95, 79, 0.12);
			border-radius: 12px;
			background: rgba(255, 253, 245, 0.8);
			transition: border-color 160ms, transform 160ms, box-shadow 160ms;
		}

		.feat-card:hover {
			border-color: var(--green);
			transform: translateY(-2px);
			box-shadow: 0 8px 24px rgba(15, 95, 79, 0.07);
		}

		.feat-icon {
			display: flex;
			align-items: center;
			justify-content: center;
			width: 44px;
			height: 44px;
			margin-bottom: 20px;
			border-radius: 10px;
			background: rgba(15, 95, 79, 0.08);
			color: var(--green);
		}

		.feat-card h3 {
			margin: 0 0 12px;
			font-size: 1.15rem;
			font-weight: 700;
			color: var(--ink);
		}

		.feat-card p {
			margin: 0;
			color: var(--muted);
			line-height: 1.65;
			font-size: 0.93rem;
		}

		/* ── CTA SECTION ─────────────────────────────────────── */
		.cta-section {
			background: linear-gradient(135deg, #0a2e24 0%, #0f5f4f 60%, #1a7a60 100%);
			padding: clamp(80px, 11vw, 140px) clamp(24px, 5vw, 80px);
			text-align: center;
		}

		.cta-inner {
			max-width: 680px;
			margin: 0 auto;
		}

		.cta-inner h2 {
			margin: 8px 0 18px;
			color: #fffdf5;
			font-size: clamp(2.4rem, 5vw, 5rem);
			font-weight: 800;
			line-height: 1.0;
			font-family: var(--font-display);
			letter-spacing: -0.025em;
		}

		.cta-sub {
			margin: 0;
			color: rgba(240, 236, 224, 0.68);
			font-size: 1.02rem;
			line-height: 1.65;
		}

		.cta-actions {
			display: flex;
			flex-wrap: wrap;
			align-items: center;
			justify-content: center;
			gap: 12px;
			margin-top: 36px;
		}

		.cta-ghost-inv {
			display: inline-flex;
			align-items: center;
			height: 48px;
			padding: 0 26px;
			background: rgba(255, 253, 245, 0.1);
			color: #fffdf5 !important;
			font-weight: 600;
			font-size: 0.95rem;
			text-decoration: none;
			border-radius: 10px;
			border: 1.5px solid rgba(255, 253, 245, 0.26);
			transition: background 150ms, border-color 150ms, transform 150ms;
		}

		.cta-ghost-inv:hover {
			background: rgba(255, 253, 245, 0.18);
			border-color: rgba(255, 253, 245, 0.55);
			transform: translateY(-2px);
		}

		.cta-link {
			color: rgba(240, 236, 224, 0.55) !important;
			font-size: 0.88rem;
			font-weight: 600;
			text-decoration: none;
			transition: color 140ms;
		}

		.cta-link:hover {
			color: #fffdf5 !important;
		}

		/* ── ANIMATIONS ──────────────────────────────────────── */
		@keyframes hero-zoom {
			from { transform: scale(1.035) translate3d(0, 0, 0); }
			to   { transform: scale(1.105) translate3d(-1.4%, -1.2%, 0); }
		}

		/* ── RESPONSIVE ──────────────────────────────────────── */
		@media (max-width: 1100px) {
			.use-cases-grid {
				grid-template-columns: repeat(2, 1fr);
			}
		}

		@media (max-width: 940px) {
			.hero {
				min-height: auto;
				padding-top: 96px;
				padding-bottom: 76px;
			}

			.hero::before {
				background:
					linear-gradient(180deg, rgba(5, 18, 15, 0.55) 0%, rgba(5, 18, 15, 0.82) 38%, rgba(5, 18, 15, 0.93) 100%),
					linear-gradient(90deg, rgba(5, 18, 15, 0.78), rgba(5, 18, 15, 0.3));
			}

			.hero-photo {
				object-position: 64% 46%;
			}

			.story-grid {
				grid-template-columns: 1fr;
			}

			.visual-sticky {
				position: relative;
				top: auto;
			}

			.chapter {
				min-height: auto;
			}

			.steps {
				grid-template-columns: 1fr;
				gap: 0;
			}

			.step-arrow {
				display: none;
			}

			.editor-section {
				grid-template-columns: 1fr;
			}

			.editor-shot {
				transform: none;
			}

			.features-grid {
				grid-template-columns: 1fr;
			}
		}

		@media (max-width: 640px) {
			.hero {
				padding-inline: 20px;
			}

			h1 {
				font-size: clamp(2.8rem, 13vw, 4rem);
			}

			.how-section {
				padding-inline: 20px;
			}

			.story-grid {
				padding-inline: 16px;
			}

			.use-cases-grid {
				grid-template-columns: 1fr;
			}

			.chapter {
				padding: 18px 16px;
			}
		}
	</style>
</SiteLayout>
